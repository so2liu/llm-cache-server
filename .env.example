# Optional, if not set, the application will use the default OpenAI URL.
OPENAI_BASE_URL=https://api.openai.com

# Optional, if not set, the application will use the Authorization header from the request.
OPENAI_API_KEY=your-api-key

# Additional base URLs (optional, separated by semicolon)
# These URLs will be tested first during auto-detection
# Example:
# ADDITIONAL_BASE_URLS=https://api.custom.com/v1;http://localhost:11434/v1

VERBOSE=false
LOG_MESSAGE=false
LOG_REQUEST_BODY=false
NEW_RELIC_LICENSE_KEY=your-new-relic-license-key
ENV=development